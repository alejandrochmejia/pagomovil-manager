# El frontend llama a /api y Vite lo reenvia al backend en dev
# (configurado en vite.config.ts -> server.proxy)
# En produccion este proxy NO existe: hay que servir front+back en el mismo origen
# o reintroducir VITE_API_URL apuntando al backend publico.
